# Диалог за 2026-09-08

Переписка с ботом: слева запросы, справа ответы. Что менялось в коде —
смотрите по коммитам за эту же дату. Вывод команд сюда не попадает.

### 06:19 · Запрос

Надо разобраться почему в этот раз запуск обработки видео занял весь процессор vps - так что пришлось перезагружать сервер.

### 06:44 · Ответ

Разобрался: процессор ни при чём — кончилась ОПЕРАТИВНАЯ ПАМЯТЬ.

Урок 66 минут. Замерил сегодня на том же файле: whisper держит 0.93 ГБ (модель 190 МБ + весь звук в памяти). На машине в тот вечер уже было занято 2.2-2.8 ГБ из 3.8 — 0.93 сверху не поместились.

22:15 (sar): свободно 76 МБ, дисковый кеш обвалился с 1.1 ГБ до 213 МБ, 1885 промахов страниц в секунду, 214 МБ/с чтения с диска. load 106 при ОДНОМ готовом к счёту процессе — всё стояло в ожидании диска, а не считало. Дальше 8 часов машина не отзывалась: даже системный сборщик статистики (раз в 10 минут) не отработал ни разу.

Ни у одного контейнера нет лимита памяти, свап всего 512 МБ — поэтому вместо падения одной задачи встал весь сервер. Повторится на следующем длинном уроке.

Подробности и что чинить — в терминале, там же вопрос с вариантами.

### 06:44 · Ответ

Что делаю с обработкой видео, чтобы сервер больше не вставал?

1️⃣ Лимит памяти воркеру в compose — падает задача, а не сервер
2️⃣ Лимит + свап до 2 ГБ (нужен ваш sudo)
3️⃣ Всё: лимит, свап и резка звука на куски по 15 минут
4️⃣ Пока ничего не менять

### 06:51 · Запрос

[Выбор пользователя] Лимит памяти воркеру в compose — падает задача, а не сервер

### 06:56 · Ответ

Готово, потолок памяти воркеру поставлен и уже применён на сервере.

mem_limit 1400 МБ (замеренные 0.93 ГБ whisper на часовом уроке плюс запас на воркер и ffmpeg), своп контейнеру запрещён — иначе он уползёт в те же 512 МБ свопа и снова потащит машину на диск.

Проверил не на словах: запустил расшифровку в контейнере с заведомо низким потолком. Ядро убило whisper внутри контейнера, воркер получил ошибку, сервер даже не заметил — load остался 1.0.

Заодно поправил текст отказа: раньше автор увидел бы в кабинете «ffmpeg завершился с кодом null», теперь «whisper убит системой: не хватило памяти» — и кнопка «Повторить» на месте.

477 тестов и линтер зелёные. Коммит не делал — скажите, и зафиксирую.

### 08:43 · Запрос

Итак - обработка прошла успешно - титры появились - но не пришло уведомление об успешном окончании обработки, хотя после успешной загрузки уведомление было.

### 08:46 · Ответ

Только что отправил вам на телефон два пробных пуша: «Проверка доставки №1» и «Проверка доставки №2». Что пришло?

1️⃣ Пришли оба
2️⃣ Пришёл только один
3️⃣ Не пришло ни одного

### 09:31 · Запрос

[Свой вариант] Какие то пуши приходили - не успел запомнить все. Нашел ошибку - после заполнения полей Заголовок, Описание нажатием на кнопку Заполнить из расшифровки - поля заполняются - но если после этого нажать на кнопку Сохранить черновик - поля опять становятся пустыми.

### 09:31 · Запрос

Заголовок
Планирование веб-портала и создание спецификации в Claude Code
Описание
Урок посвящен детальному планированию сложной системы для автоматизации работы с видеоконтентом. С помощью Claude Code формируется техническое задание, выбирается стек технологий и создается структура будущего приложения. После изучения материала получится составить качественную спецификацию и подготовить окружение для разработки проекта.
Теги через запятую
claude code, telegram bot, vps, github, pwa, планирование, спецификация

### 09:37 · Ответ

Нашёл причину: форма проверки урока не перехватывалась клиентом и уходила GET-ом — поля уезжали в адресную строку, а страница перечитывалась из базы пустой. Исправил, тесты зелёные. Выкатывать? В дереве лежат ещё ваши правки стилей с прошлой сессии (кегль полей на телефоне, important) — сборка возьмёт и их.

1️⃣ Выкатывай всё вместе
2️⃣ Только исправление формы
3️⃣ Пока не выкатывай

### 09:37 · Запрос

[Выбор пользователя] Выкатывай всё вместе

### 09:39 · Ответ

Выкачено, проверил на живом сайте.

Причина была такая: у формы проверки урока не было ни action, ни перехвата в клиенте — браузер отправлял её GET-ом на тот же адрес. Ваш заголовок уезжал в адресную строку (это видно в журнале nginx), страница открывалась заново и рисовалась из базы, где ничего не сохранено. Отсюда и пустые поля.

Теперь «Сохранить черновик» и «Опубликовать» шлют поля на сервер как положено. Заодно поставил сторожа на весь класс ошибки: тест проверяет каждую форму на экранах — либо она шлёт себя сама, либо её отправку перехватывает клиент. До исправления он назвал ровно эту форму, сейчас зелёный.

Вместе с этим уехали ваши правки стилей с прошлой сессии (кегль полей на телефоне) — проверил, на сайте они есть. И воркер теперь работает с новым текстом отказа про нехватку памяти.

478 тестов и линтер зелёные. Попробуйте заполнить и сохранить — должно остаться на месте. Коммит по-прежнему не делал.

### 09:43 · Запрос

черновик урока подготовлен. далее необходимо его опубликовать - нажимаю на кнопку Опубликовать? Напоминаю что мне необходимо чтобы она автоматически загружался на платформы youtube/rutube/vkvideo/dzen

### 09:45 · Ответ

Да, жмите — это безопасно и обратимо.

Что делает кнопка сейчас: ставит урок на витрину портала, проставляет дату выхода, шлёт пуш подписчикам (один раз — повторное сохранение никого не разбудит второй раз) и снимает пометку обработки. Передумаете — «Сохранить черновик» вернёт урок в черновики.

Ни на YouTube, ни на RuTube, ни в VK, ни в Дзен при этом ничего не уходит. Автозагрузки на площадки в портале нет вообще: по вашей же спецификации это этапы 7 (YouTube) и 8 (остальные), они не начинались. Готово только основание — таблица публикаций со статусами и ссылками и кнопки площадок на карточке урока; адаптеров нет ни одного.

Важный срок: исходник живёт в буфере 7 суток. L2-2.mp4 (705 МБ) лежит с сегодняшнего утра, то есть до 15 сентября. После этого для выкладки его придётся заново тянуть с Яндекс Диска.

Подробности по каждой площадке — в терминале. Там же вопрос, с чего начинать.

### 09:45 · Ответ

Браться за автозагрузку на площадки? Делать надо по одной — площадка не считается сделанной, пока настоящий ролик туда не уехал.

1️⃣ Да, начинаем с YouTube (этап 7)
2️⃣ Начать с VK Video — он проще по доступу
3️⃣ Сначала обсудим объём и порядок
4️⃣ Не сейчас

### 09:47 · Запрос

[Выбор пользователя] Да, начинаем с YouTube (этап 7)

### 09:49 · Ответ

Прежде чем проектировать — главное ограничение, я его проверил по документации Google, а не по памяти.

Ролики, залитые через API приложением, которое не прошло проверку Google (compliance audit), ПРИНУДИТЕЛЬНО остаются приватными. Поле «сделать публичным» при загрузке просто игнорируется. Это правило для всех проектов, заведённых после 28 июля 2020, и оно действует сейчас. Снимается только аудитом: заявка, описание сценария, демо-видео вашего OAuth-экрана, обычно недели.

Что при этом НЕ мешает: квота. По умолчанию 100 загрузок в сутки плюс 10 000 единиц на всё остальное (субтитры, обложка). Для урока в неделю это с огромным запасом.

То есть сегодня портал может залить ролик со всеми полями, субтитрами и обложкой, но опубликовать его должны вы — одним нажатием в студии. Ровно этот случай в вашей спецификации называется режимом semi.

### 09:49 · Ответ

Как поступаем с этим ограничением?

1️⃣ Делаем semi сейчас, заявку на аудит подаёте параллельно (рекомендую)
2️⃣ Делаем semi, аудит не подаём — дожимаете руками всегда
3️⃣ Ждём аудит, код пишем после

### 09:50 · Запрос

[Выбор пользователя] Делаем semi сейчас, заявку на аудит подаёте параллельно (рекомендую)

### 09:51 · Ответ

Вопрос второй: какой файл уезжает на YouTube? Сейчас у урока есть только исходник — монтаж с вырезанными паузами вы для него не собирали (кнопка «Сохранить и пересобрать» в настройках).

1️⃣ Монтаж, если собран; иначе исходник (рекомендую)
2️⃣ Всегда исходник как записан
3️⃣ Только монтаж — без него на площадку не пускать

### 09:52 · Запрос

[Свой вариант] Монтаж, если собран; иначе исходник - также необходимо учитывать субтитры отдельно от видео или уже наложенные на видео.

### 09:53 · Ответ

Про субтитры — принято, и в портале для этого уже есть половина нужного.

Как будет: на YouTube субтитры уезжают ОТДЕЛЬНЫМ треком, а не вшиваются в кадр. Так зритель их выключает, они видны на телевизоре, и YouTube умеет по ним переводить. Вшитые ничем из этого не будут.

Ваш случай «подписи уже на записи» портал знает: в настройках урока есть галка «Есть ли на записи уже наложенные подписи» — сейчас она гасит вшивание подписей в вертикальные ролики. Адаптер будет читать её же: галка стоит — отдельный трек не грузим, иначе на экране получатся две строки подписей друг под другом.

И ещё одна тонкость, которую нашёл в коде: монтаж сдвигает времена, поэтому шаг вырезания пауз кладёт СВОИ субтитры (trimmed.srt) рядом со смонтированным роликом. Значит трек берётся под тот файл, который уехал: монтаж — trimmed.srt, исходник — subtitles.srt. Перепутать их значит получить подписи, которые к концу урока опаздывают на минуты.

### 09:53 · Ответ

Вопрос третий: чем запускается выкладка на YouTube?

1️⃣ Отдельной кнопкой на экране урока (рекомендую)
2️⃣ Автоматически по кнопке «Опубликовать» на портале
3️⃣ Автоматически, но только для уже опубликованных уроков

### 09:55 · Запрос

[Выбор пользователя] Отдельной кнопкой на экране урока (рекомендую)

### 09:55 · Ответ

Вопрос четвёртый, последний: главы в описании ролика (00:00 Вступление, 04:12 …). В критерии приёмки этапа 7 они есть, но готового шага под них нет — расшифровка это реплики по 5-10 секунд, из них главы надо собирать отдельно, скорее всего той же моделью Gemini, что предлагает заголовок. Это заметный кусок работы сверх самой выкладки.

1️⃣ Сначала выкладка без глав, главы следом отдельно (рекомендую)
2️⃣ Делаем сразу с главами — этап не считается сданным без них
3️⃣ Главы не нужны вовсе

### 09:57 · Запрос

[Выбор пользователя] Сначала выкладка без глав, главы следом отдельно (рекомендую)

### 09:58 · Ответ

Итог опроса: режим semi (ролик уезжает приватным, публикуете вы), файл — монтаж если собран иначе исходник, субтитры отдельным треком с оглядкой на галку «подписи уже на записи», запуск отдельной кнопкой, главы — потом.

Дальше два решения принимаю сам, скажите, если не согласны:

1) Пишу на голом fetch, без пакета googleapis. Причина: Яндекс Диск в портале уже сделан так же, а googleapis тянет полсотни мегабайт зависимостей ради четырёх запросов.

2) Загрузка идёт по «возобновляемому» протоколу YouTube и потоком с диска, а не целиком в память. Причина прямая: у воркера теперь потолок 1.4 ГБ, и попытка подержать в памяти семисотмегабайтный файл убьёт его — ровно тем способом, от которого мы утром ставили предохранитель.

Остался один настоящий выбор — где выполняется заливка.

### 09:58 · Ответ

Где выполняется выкладка на площадку?

1️⃣ Шагом в той же очереди, что и обработка (рекомендую — проще, всё единообразно)
2️⃣ В отдельной очереди для площадок — заливка не займёт слот обработки

### 09:58 · Запрос

[Выбор пользователя] Шагом в той же очереди, что и обработка (рекомендую — проще, всё единообразно)

### 10:00 · Ответ

Устройство сложилось. Коротко, полностью — в терминале.

ПОДКЛЮЧЕНИЕ. Отдельный проект Google Cloud, кнопка рядом с Яндекс Диском, одна область доступа youtube.force-ssl (покрывает и ролик, и субтитры, и обложку). Токены в готовую таблицу integrations, зашифрованными.

ВЫКЛАДКА. Кнопка «Отправить на YouTube» → задача в очередь. Уезжает: монтаж или исходник, субтитры в пару именно к нему, заголовок с обрезкой на 100 знаках, описание со ссылкой на урок, теги, обложка. Обязательные поля, без которых YouTube отказывает: категория «Образование», язык, «не для детей».

Про обложку не теория: выбранная вами для урока 15 весит 2 040 881 байт при пределе 2 МБ — впритык. Тяжелее 1.9 МБ буду пережимать через ffmpeg.

ЧЕСТНАЯ ССЫЛКА. Ролик уезжает приватным, поэтому после загрузки состояние «лежит, но не публичен», и ссылка на площадку в карточке урока НЕ показывается — иначе зритель жмёт кнопку в никуда. В кабинете — ссылка в студию и кнопка «Проверить»: она спрашивает у YouTube, открыли ли вы ролик, и тогда ссылка появляется. После аудита переключение в режим auto меняет ровно одну вещь — ролик сразу публичный.

ОШИБКИ. Причина на экране, уведомление, повтор той же кнопкой, оборванная заливка продолжается с места обрыва. Исчерпанную квоту называю квотой, а не поломкой.

### 10:00 · Ответ

Устройство такое. Что дальше?

1️⃣ Всё так — пиши спеку
2️⃣ Есть замечания, скажу своими словами

### 10:04 · Запрос

[Выбор пользователя] Всё так — пиши спеку

### 10:06 · Ответ

Спека написана: docs/superpowers/specs/2026-09-08-youtube-publishing-design.md, 213 строк.

Против того, что я вам рассказал, добавились три уточнения — нашёл их, перечитывая свежим взглядом:

1. Кнопка не просто гаснет, а называет причину: канал не подключён / видео нет / выкладка уже идёт. Погасшая кнопка без объяснения заставляет искать причину в себе.

2. Повтор переписывает строку публикации, а не заводит вторую. Иначе в карточке две ссылки на один ролик и не понять, какая настоящая.

3. Режим (semi/auto) хранится в строке публикации, а значение по умолчанию — в переменной окружения YOUTUBE_MODE. После аудита вы меняете одну переменную, кода никто не трогает.

Открытых вопросов в спеке нет.

### 10:06 · Ответ

Спеку надо прочитать до плана работ. Как удобнее?

1️⃣ Пришли её сюда текстом, прочитаю с телефона
2️⃣ Прочитаю сам в репозитории — подожди
3️⃣ Читать не буду, пиши план работ

### 10:09 · Запрос

[Свой вариант] хотел прочитать в github но не нашел файла в репозитории

### 10:11 · Ответ

Отправил на GitHub, спека здесь:

https://github.com/boriscom123/my_portal/blob/main/docs/superpowers/specs/2026-09-08-youtube-publishing-design.md

Заодно ушло всё, что накопилось за сегодня, пятью отдельными коммитами:

1. потолок памяти воркеру + внятный текст отказа по памяти
2. кегль полей на телефоне (ваша правка с прошлой сессии)
3. форма проверки урока больше не стирает поля + сторож на весь класс ошибки
4. спека этапа 7
5. переписка за 7 и 8 сентября

Ветку не заводил, положил в main — как все ваши предыдущие коммиты, иначе на главной странице репозитория файла бы не было.

Прочитаете — скажите, и берусь за план работ.

### 10:14 · Запрос

продолжаем с планом работ

### 10:16 · Ответ

Стоп на минуту — нашёл то, что должен был найти раньше.

В репозитории лежит план этапа 7а от 4 сентября: «основание публикации», 2489 строк, десять задач. Он не исполнен ни на строку — ни реестра площадок, ни страницы настроек, ни миграции из него в коде нет.

Он шире того, что мы вчетвером наговорили сегодня: там сразу восемь площадок, интерфейс адаптера на всех, страница настроек, ручные площадки с пакетом материалов и публикация вертикальных нарезок. YouTube в нём отложен во «вторую порцию».

Наша сегодняшняя спека уже другая: одна площадка, semi, кнопка на экране урока. Это не противоречие в вашей голове, а два разных решения, принятых в разные дни, и выбрать надо одно — иначе в репозитории останутся два документа, спорящих друг с другом.

### 10:16 · Ответ

Что делаем с планом 7а от 4 сентября?

1️⃣ Пишу узкий план под YouTube, 7а помечаю отложенным (рекомендую)
2️⃣ Сначала исполняем 7а целиком, YouTube после него
3️⃣ Перепиши 7а целиком под сегодняшние решения

### 10:19 · Запрос

[Свой вариант] Площадок будет много - просто мы начали с youtube. под каждую на данном этапе подготавливаем обработчики с отдельным запуском. В последующем объединим запуск.

### 10:29 · Ответ

План работ готов и уже на GitHub:

https://github.com/boriscom123/my_portal/blob/main/docs/superpowers/plans/2026-09-08-youtube-publishing.md

Девять задач, 2230 строк, у каждой свой тест и своя фиксация:
1. миграция: публикация привязана к файлу + состояние «лежит, но не публичен»
2. состояния публикации в одном месте
3. подключение канала (OAuth, обновление токена)
4. что уезжает: выбор файла, субтитры в пару к нему, поля ролика
5. пережатие обложки под предел площадки
6. запросы к YouTube: загрузка потоком, субтитры, обложка
7. шаг очереди — выкладка целиком
8. кнопка, состояния на экране, честная ссылка в карточке
9. живая приёмка на вашем канале + памятка по настройке Google Cloud

Самопроверка нашла три моих же ошибки, все исправлены: план ссылался на тестового помощника, которого в проекте нет; неверно звал createApp; и обещал добавить в карточку правило, которое там уже написано с этапа 2.

Из плана 7а взял одно решение — публикацию привязываем к файлу, а не к уроку. Сегодня это бесплатно, а завтра снимает миграцию, когда на площадку коротких видео поедут три вертикальных ролика.

Дальше — исполнение. Скажите, когда.

### 10:31 · Запрос

делаем реализацию

### 10:37 · Ответ

Три задачи из девяти готовы, 491 тест зелёный.

1. Миграция: публикация привязана к файлу, появилось состояние «лежит приватным».
2. Состояния публикации — в одном месте, повтор переписывает строку и стирает прежнюю ошибку.
3. Подключение канала: обмен токенами с Google, самообновляющийся токен, кнопка «Подключить YouTube» на странице загрузки.

Работаю в отдельной ветке youtube-publishing — держать девять задач в дереве, из которого собирается живой сайт, нельзя. В main вольётся, когда всё пройдёт проверку.

Дальше: что уезжает на площадку (выбор файла и субтитров в пару к нему).

### 10:41 · Ответ

Шесть задач из девяти. Готово: миграция, состояния, подключение канала, выбор файла и субтитров, пережатие обложки, запросы к площадке.

Один тест поймал мою ошибку и это стоило того: я собирался брать субтитры по имени видеофайла. У монтажа имена совпадают (trimmed.mp4 и trimmed.srt), а у исходника нет — он называется как ваш файл (L2-2.mp4), а субтитры к нему конвейер кладёт под именем subtitles.srt. По имени вышло бы, что к исходнику субтитры не находятся вовсе. Теперь пара выбирается по виду файла.

Осталось: шаг очереди, кнопка на экране урока, живая приёмка.

### 10:42 · Запрос

доделывай

### 10:54 · Ответ

Все девять задач исполнены, 528 тестов и линтер зелёные. Ветка youtube-publishing отошла от main. Что делаем?

1️⃣ Влить в main и выкатить на сервер
2️⃣ Влить в main, но не выкатывать
3️⃣ Оставить ветку как есть

### 10:58 · Запрос

[Выбор пользователя] Влить в main и выкатить на сервер

### 11:01 · Ответ

Влито в main и выкачено. Проверил на живом сервере:

— миграция применилась сама при запуске: «Применены миграции: 014_publications_youtube.sql», в таблице публикаций появились колонка файла, состояние ready и уникальность с NULLS NOT DISTINCT;
— портал отвечает, новые кнопки в живом клиенте на месте;
— 528 тестов и линтер зелёные уже на слитом дереве, а не только на ветке.

Для зрителя не изменилось ничего: раздел «Площадки» на экране урока молчит, пока в .env нет ключей Google.

ЧТО ДЕЛАТЬ ВАМ, по порядку — подробно в docs/youtube-setup.md, он в репозитории:
1. Завести ОТДЕЛЬНЫЙ проект в Google Cloud (не тот, где вход через Google) и включить YouTube Data API v3.
2. Экран согласия: тип External, себя в тестовые пользователи, одна область youtube.force-ssl.
3. Учётные данные типа «Веб-приложение», адрес возврата https://soloaijourney.online/api/integrations/youtube/callback — до последнего знака.
4. Прислать мне Client ID и секрет, либо вписать самому в .env вместе с YOUTUBE_REDIRECT_URI и YOUTUBE_MODE=semi, и я перезапущу.
5. Дальше: «Подключить YouTube» на странице загрузки и «Отправить на YouTube» на экране урока.

Отдельно стоит подать заявку на аудит Google — она снимает принудительную приватность. Пока её нет, ролик уезжает приватным, и открывать его в студии придётся вам.

### 11:04 · Запрос

4. Прислать мне Client ID и секрет, либо вписать самому в .env вместе с YOUTUBE_REDIRECT_URI и YOUTUBE_MODE=semi, и я перезапущу. - эти данные я хочу добавить через раздел - Настройки. - там надо добавить отдельный раздел настроек для интеграции с youtube

### 11:05 · Ответ

Понял: ключи вводите в кабинете, не в .env. Устройство короткое.

ГДЕ. Новый раздел «Площадки» на странице Настройки, виден только вам. Там как раз стоит заглушка «подключения площадок появятся здесь».

ЧТО В НЁМ. Поле Client ID; поле секрета, которое пишется, но не показывается (сохранён — значит «сохранён», можно заменить); адрес возврата готовой строкой для копирования в Google Cloud — спрашивать его незачем, он наш и вычисляется из адреса портала; переключатель «ролик приватным / сразу публичным» на после аудита; и тут же состояние: подключён канал или нет, с кнопкой подключения.

ГДЕ ЛЕЖИТ. Отдельная таблица приложений площадок, секрет зашифрован тем же ключом, что и токены: дамп базы без ключа бесполезен. Не в таблице подключений — там связь с конкретным каналом, а это само приложение, оно существует до всякой связи.

ЧТО МЕНЯЕТСЯ В КОДЕ. Сейчас ключи читаются из окружения один раз при старте. Раз их меняют в кабинете, спрашивать их придётся в момент работы — это правка в четырёх местах: экран согласия, обмен кода, обновление токена, шаг выкладки. Значения из .env останутся запасным вариантом: пусто в базе — берём из окружения, так уже выкаченный портал не сломается.

Секрет наружу не отдаётся ни страницей, ни API.

### 11:05 · Ответ

Делаю так?

1️⃣ Да, делай
2️⃣ Есть замечания

### 11:23 · Запрос

[Выбор пользователя] Да, делай

### 11:33 · Ответ

Готово и уже на сервере. Ключи вводятся в кабинете: Настройки → Площадки → YouTube.

Что там:
— Client ID и Client secret. Секрет шифруется и обратно не показывается никогда: при следующей правке поле будет пустым, и пустым его можно оставить — сохранённый не потеряется.
— Адрес возврата НЕ спрашивается: портал считает его сам и показывает готовой строкой — скопируйте её в Google Cloud. Одним полем меньше и одной опечаткой меньше там, где опечатка даёт redirect_uri_mismatch.
— Выбор «оставлять приватным / публиковать сразу». Второй заработает только после проверки приложения в Google.
— Там же кнопка подключения канала, а после подключения — отключения. Отключение забывает токены, но ключи приложения оставляет: заводить их заново ради смены аккаунта незачем.

Переменные YOUTUBE_* в .env больше не нужны — остались запасным путём, если в кабинете пусто.

540 тестов и линтер зелёные, миграция применилась на сервере сама, памятка docs/youtube-setup.md переписана под новый порядок.

Можно заводить приложение в Google Cloud и вводить ключи.

### 11:47 · Запрос

необходимо сделать нормальные стили для блоков input и select

### 11:48 · Ответ

Причину видно по скриншоту и по стилям.

Оформление полей в портале написано ШЕСТЬ раз — отдельно под каждую форму, по её идентификатору: #review-form input, #idea-form input, #upload-form select, #comment-form textarea, #search-form input, .segment input. Правила почти одинаковые. Новая форма в настройках ни под одно не попала — отсюда и голые поля, и то, что подписи встали в строку.

Свой седьмой блок писать не буду: тогда следующая форма сломается ровно так же. Предлагаю одно общее правило на поля ввода, списки и многострочные поля — ровно тем видом, что уже есть у остальных: заливка цветом фона, рамка в линию, скругление, отступ, минимальная высота 44 точки и обводка при фокусе. Плюс подписи в формах встают над полем, а не рядом.

Дубликаты при этом уберу: они и есть причина. Особенное останется особенным — поле поиска, выбор файла, строки титров, галочки.

Оговорка честная: это касается ВСЕХ форм портала, а не только настроек. Внешне они не должны измениться — правило то же самое, — но проверю каждую страницу после.

### 11:48 · Ответ

Как делать?

1️⃣ Общее правило и убрать дубликаты (рекомендую)
2️⃣ Только раздел настроек, остальное не трогать

### 12:02 · Запрос

[Выбор пользователя] Общее правило и убрать дубликаты (рекомендую)

### 12:09 · Ответ

Готово, стили выкачены — обновите страницу настроек (может понадобиться жёсткое обновление, стили кешируются с отпечатком).

Что теперь: подпись стоит НАД полем, поля с рамкой, скруглением и отступом, высотой не меньше 44 точек, с обводкой при фокусе. Выпадающий список выглядит так же, как поля.

Дубликаты убрал: было шесть почти одинаковых блоков под каждую форму, стало одно правило на портал. Оно написано по именам тегов и потому слабее правил по классу — особенное осталось особенным: поле поиска тянется в строке, строки титров сидят плотно (иначе тысяча реплик превратилась бы в бесконечную ленту), выбор файла со своей кнопкой не тронут, галочки стоят рядом с подписью.

541 тест зелёный, добавил сторожа: если общее правило снесут, тест это поймает.

Посмотрите, пожалуйста, не только настройки, но и форму отзыва под уроком, борд идей, поиск и экран проверки — правка общая, и я хочу знать, если где-то стало хуже.

### 16:34 · Запрос

не понимаю как сделать этот шаг - 2. Экран согласия
«APIs & Services» → «OAuth consent screen»:

тип External;
название приложения, ваша почта для связи;
область доступа — одна: https://www.googleapis.com/auth/youtube.force-ssl. Она покрывает и загрузку ролика, и субтитры, и обложку;
в «Test users» добавьте себя — тот аккаунт, которому принадлежит канал.
Пока приложение в тестовом режиме, доступ работает только для тестовых пользователей. Для канала одного автора этого достаточно.

### 16:36 · Ответ

Моя памятка отстала от жизни — Google переименовал этот экран. «APIs & Services → OAuth consent screen» больше нет, теперь это «Google Auth Platform». Проверил по документации, порядок такой.

Слева меню (три полоски) → Google Auth Platform. Или в поиске сверху наберите Auth Platform. Если проект новый — там будет кнопка «Get started».

Дальше четыре раздела, слева:

1) BRANDING — имя приложения (любое, хоть «Solo AI Journey»), ваша почта поддержки, ниже «Developer contact information» — снова ваша почта. Save.

2) AUDIENCE — User type: External. Save. Ниже блок «Test users» → «Add users» → ваша почта, та самая, которой принадлежит канал. Save.

3) DATA ACCESS — кнопка «Add or remove scopes». В поле фильтра вставьте youtube.force-ssl, отметьте галкой строку .../auth/youtube.force-ssl → Update → Save. Google предупредит, что это чувствительная область доступа, — так и есть, для этого потом и нужен аудит.

4) CLIENTS — это уже следующий шаг памятки: Create client → Web application → адрес возврата.

ВАЖНОЕ, чего в памятке не было. Пока приложение в статусе Testing, Google убивает доступ через 7 дней — подключать канал пришлось бы каждую неделю. Поэтому в разделе Audience нажмите «Publish app» и подтвердите: статус станет «In production». При подключении Google один раз покажет страшное предупреждение «Google hasn't verified this app» — нажмите Advanced и «Go to …». Это не тот аудит, что снимает приватность роликов, это другое и вас не касается.

Памятку в репозитории сейчас перепишу под эти названия.

### 16:43 · Запрос

вроде все добавил - при попытке авторизации получаю ошибку - Доступ заблокирован: приложение "soloaijourney.online" не прошло проверку Google

boriscom12345@gmail.com
Мы пока не проверили приложение "soloaijourney.online". Сейчас оно тестируется и им могут пользоваться только одобренные разработчиком тестировщики. Если вы считаете, что у вас должен быть доступ к приложению, свяжитесь с разработчиком.
Если вы разработчик приложения "soloaijourney.online", прочитайте подробную информацию об ошибке.
Ошибка 403: access_denied

### 16:44 · Ответ

Эта ошибка означает одно: приложение всё ещё в статусе Testing, а ваша почта в списке тестировщиков не сохранилась (или сохранилась в другом проекте).

Самое быстрое лечение — не возиться со списком, а убрать сам режим тестирования:

Google Auth Platform → AUDIENCE → вверху блок «Publishing status». Если написано Testing — кнопка «PUBLISH APP» → подтвердить. Статус станет «In production». Пробуйте подключение заново.

Заодно это снимает то, о чём я писал: в режиме Testing Google убивает доступ через 7 дней, и подключать канал пришлось бы каждую неделю.

При подключении Google один раз покажет «Google hasn't verified this app» — это нормально для непроверенного приложения: «Advanced» → «Go to soloaijourney.online (unsafe)». Слово unsafe тут про отсутствие проверки Google, а не про опасность: приложение ваше и живёт на вашем же сервере.

ЕСЛИ КНОПКИ PUBLISH APP НЕТ или ошибка осталась — проверьте вверху страницы, ТОТ ЛИ ПРОЕКТ выбран. Частая история: экран согласия настроен в одном проекте, а Client ID создан в другом — тогда Google смотрит в чужой список тестировщиков. Название проекта в шапке консоли должно быть одно и то же на вкладках Audience и Clients.

Если и это совпадает — пришлите скриншот вкладки Audience целиком, разберёмся по нему.

### 16:45 · Запрос

не могу перевести в режим - Publishing status - Your app's OAuth configuration is incomplete. You must enter the missing information to proceed. Please visit the Branding page to finish configuring your app.

### 16:46 · Ответ

Понятно, чего не хватает. Для перевода в production Google требует на вкладке BRANDING заполнить блок «App domain» целиком:

— Application home page: https://soloaijourney.online
— Application privacy policy link: страница политики конфиденциальности
— Application terms of service link: страница условий использования
— Authorized domains: soloaijourney.online
— и ниже Developer contact information — ваша почта

Первый и последний пункты у вас есть. Загвоздка в двух средних: таких страниц на портале нет вовсе, а Google их проверяет — ссылка должна открываться и вести на ваш же домен. Пустыми эти поля оставить нельзя, чужие подставить тоже: проверяющий робот ходит по ним.

Предлагаю сделать их прямо сейчас — две простые страницы на портале, /privacy и /terms, со ссылками в подвале сайта. Текст напишу по существу вашего портала, без юридической воды: какие данные собираются (вход через Google и Telegram, реакции, отзывы, идеи), зачем, где хранятся, как их удалить, и что портал делает с доступом к вашему же каналу YouTube. Это и Google устроит, и посетителю честно.

Полчаса работы. Делаю?

### 16:46 · Ответ

Страницы политики и условий — делаем?

1️⃣ Да, делай и выкладывай
2️⃣ Сделай, но текст сначала покажи мне
3️⃣ Не надо, напишу сам

### 16:46 · Запрос

[Выбор пользователя] Да, делай и выкладывай

### 16:54 · Ответ

Страницы готовы и уже на сайте — проверил, обе отдаются гостю:

https://soloaijourney.online/privacy
https://soloaijourney.online/terms

Теперь заполните на вкладке BRANDING блок «App domain»:

Application home page: https://soloaijourney.online
Application privacy policy link: https://soloaijourney.online/privacy
Application terms of service link: https://soloaijourney.online/terms
Authorized domains: soloaijourney.online

Сохраните — и «Publish app» на вкладке Audience должна нажаться.

Что в текстах: только то, что портал правда делает. Почту он не хранит вовсе — вход через Google её сообщает, но в базу она не попадает; следящих счётчиков нет; про доступ к вашему каналу написано отдельно — загружает ваши же записи, чужого не читает и не удаляет, токен лежит зашифрованным. Ссылки на обе страницы встали в подвал сайта.

Заодно поправил текст ошибки: если доступ к YouTube протухнет, вы увидите «подключение истекло — подключите канал заново», а не английское invalid_grant. Это не теория: пока приложение числится тестовым, Google убивает доступ каждые семь дней — ещё одна причина нажать Publish app.

Памятку переписал под нынешнюю консоль: новые названия вкладок, обязательная публикация приложения и разбор обеих ошибок, на которые вы наткнулись. 545 тестов зелёные.

### 17:14 · Запрос

И еще создай логотип для прикрепления - чтобы я мог его по ссылке скачать

### 17:16 · Ответ

Логотип готов, скачивается по ссылке:

https://soloaijourney.online/icons/logo-120.png

Это ваш же знак — ракета на тёмном, собран из того же вектора, что и иконки приложения, чтобы они не разошлись. Ровно под требования Google: квадрат 120 точек, PNG, 2.6 КБ при пределе в мегабайт.

Если понадобится крупнее — https://soloaijourney.online/icons/icon-512.png, тот же знак 512 точек.

На вкладке Branding поле называется «App logo», рядом с именем приложения: скачайте файл и загрузите его туда.

