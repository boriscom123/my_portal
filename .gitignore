# Секреты и локальная конфигурация — репозиторий публичный, сюда ничего не попадает
.env
.env.*
!.env.example
*.pem
*.key

# Рабочий буфер конвейера: исходники, нарезки, субтитры, обложки
media/
tmp/

# Служебные файлы блокировки, которые создаёт запись диалога
docs/history/*.lock

node_modules/
npm-debug.log*

.remember/
