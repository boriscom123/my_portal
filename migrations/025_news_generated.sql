-- Служебные данные новости: отметка «картинка рисуется», запрос, по которому
-- она рисуется, и отказ рисования.
--
-- То же, что lessons.generated у урока: черновик для автора, а не данные, по
-- которым ищут. Отдельной таблицы не заводим — у новости это три поля, и
-- живут они ровно столько же, сколько сама новость.
-- Читается из src/services/news.js и src/services/cover-drawing.js.
ALTER TABLE news ADD COLUMN generated jsonb NOT NULL DEFAULT '{}'::jsonb;
