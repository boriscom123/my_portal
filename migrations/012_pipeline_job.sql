-- Упавшая задача целиком: имя шага и его данные.
--
-- Задача — дать кнопке «Повторить» в кабинете точно знать, что повторять.
-- Зачем не разбирать имя шага из текста ошибки: текст пишется для человека и
-- однажды поменяется, а повтор молча начнёт запускать не тот шаг. Зачем не
-- только имя: шаг «забрать с Диска» без пути к файлу повторить нельзя, а путь
-- живёт только в данных задачи.
-- Читается из src/routes/admin.js, пишется из src/worker.js.
ALTER TABLE lessons ADD COLUMN pipeline_job jsonb;
