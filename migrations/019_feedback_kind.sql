-- Вид обращения: идея, пожелание, отзыв.
--
-- Раздел «Идеи» становится «Обратной связью»: по сути это одно и то же —
-- человек говорит автору, чего хочет, — и разница лишь в том, про что. Держать
-- под это три таблицы значило бы трижды написать одно и то же голосование,
-- модерацию и список.
--
-- Голосование осмысленно только у идей: голосовать за чужой отзыв бессмысленно,
-- и это правило живёт в коде, а не здесь — база лишь хранит вид.
-- Читается из src/services/ideas.js.
ALTER TABLE ideas ADD COLUMN kind text NOT NULL DEFAULT 'idea'
  CHECK (kind IN ('idea', 'wish', 'review'));
